---
phase: 02-settings-management-propagation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - tests/core.test.cjs
autonomous: true
requirements:
  - GEM-05-01
must_haves:
  truths:
    - "Manual updates to GSD model profiles are immediately propagated to .gemini/settings.json"
    - "Sync is triggered lazily when GEMINI_CLI=1"
  artifacts:
    - path: get-shit-done/bin/lib/core.cjs
      provides: Lifecycle hook for syncGeminiSettings
  key_links:
    - from: get-shit-done/bin/lib/core.cjs
      to: syncGeminiSettings
      via: Invocation inside resolveModelInternal
---

<objective>
Trigger synchronization of `.gemini/settings.json` dynamically when Gemini CLI is active.

Purpose: To fulfill the "lazy sync" requirement without recursing infinitely or slowing down GSD by syncing on every call.
Output: Integrated `syncGeminiSettings` trigger in `resolveModelInternal`.
</objective>

<context>
@get-shit-done/bin/lib/core.cjs
@tests/core.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire sync trigger in resolveModelInternal</name>
  <files>get-shit-done/bin/lib/core.cjs, tests/core.test.cjs</files>
  <behavior>
    - If `process.env.GEMINI_CLI === '1'`, `!options.skipSync`, and it has not already synced in the current process, it calls `syncGeminiSettings(cwd)`.
    - Sets a `hasSynced = true` flag to prevent multiple syncs in a single process.
    - Test ensures `syncGeminiSettings` is called exactly once when multiple models are resolved sequentially.
  </behavior>
  <action>
    Add `let hasSynced = false;` module-level variable to `core.cjs`.
    Update `resolveModelInternal` to trigger `syncGeminiSettings(cwd)` when `process.env.GEMINI_CLI === '1'`, `!options.skipSync`, and `!hasSynced`.
    After calling it, set `hasSynced = true`.
    Export a test helper `_resetSyncFlag()` to reset `hasSynced` for testing.
    Update `tests/core.test.cjs` to verify that `syncGeminiSettings` executes only once per process lifecycle unless reset, and that manual config overrides (which change resolutions) reflect on the next sync.
  </action>
  <verify>
    <automated>node tests/core.test.cjs</automated>
  </verify>
  <done>Sync trigger works, tests pass, no recursion happens, only triggers when GEMINI_CLI=1.</done>
</task>

</tasks>

<verification>
  <automated>node tests/core.test.cjs</automated>
</verification>

<success_criteria>
Lazy sync is fully operational.
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-management-propagation/02-02-SUMMARY.md`
</output>
