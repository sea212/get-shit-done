---
phase: 02-model-resolution-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - tests/core.test.cjs
  - get-shit-done/templates/config.json
  - get-shit-done/bin/gsd-tools.cjs
  - get-shit-done/bin/lib/commands.cjs
autonomous: true
requirements:
  - MOD-01
  - MOD-02
  - MOD-03
  - PROF-01
  - PROF-02
must_haves:
  truths:
    - "Anthropic tiers are mapped to correct Gemini models when in Gemini CLI"
    - "Overrides using Anthropic tiers are also mapped"
    - "Native Gemini model overrides are passed through untouched"
    - "User is notified of mapping at startup"
    - "Progress indicators show mapped model"
  artifacts:
    - path: get-shit-done/bin/lib/core.cjs
      provides: "Model resolution mapping intercept and config parsing"
    - path: tests/core.test.cjs
      provides: "Unit tests for model mapping"
    - path: get-shit-done/templates/config.json
      provides: "Default mapping configuration"
    - path: get-shit-done/bin/gsd-tools.cjs
      provides: "CLI progress indicators showing mapped models"
    - path: get-shit-done/bin/lib/commands.cjs
      provides: "CLI command handling for model visibility"
  key_links:
    - from: get-shit-done/bin/lib/core.cjs
      to: process.env.GEMINI_CLI
      via: isGeminiEnvironment
---

<objective>
Implement dynamic model mapping to replace standard Anthropic model tier strings ('opus', 'sonnet', 'haiku') with their corresponding Gemini preview and lite versions.
Purpose: Allow GSD to seamlessly operate in `gemini-cli` environments while respecting the user's base model configurations, with user feedback and status visibility.
Output: Modified core library, UI visibility updates, config defaults, and passing test suites.
</objective>

<context>
@get-shit-done/bin/lib/core.cjs
@tests/core.test.cjs
@get-shit-done/templates/config.json
@get-shit-done/bin/gsd-tools.cjs
@get-shit-done/bin/lib/commands.cjs
</context>

<tasks>

<task type="auto" tdd="true" status="completed">
  <name>Task 1: Update Tests for Model Resolution</name>
  <files>tests/core.test.cjs</files>
  <behavior>
    - When GEMINI_CLI=1, 'opus' and 'claude-3-opus' resolve to 'gemini-3.1-pro-preview' (MOD-01)
    - When GEMINI_CLI=1, 'sonnet' and 'claude-3-5-sonnet' resolve to 'gemini-3-flash-preview' (MOD-02)
    - When GEMINI_CLI=1, 'haiku' and 'claude-3-5-haiku' resolve to 'gemini-2.5-flash-lite' (MOD-03)
    - When GEMINI_CLI=1, explicit override 'gemini-1.5-pro-002' stays 'gemini-1.5-pro-002' (PROF-01, PROF-02)
    - Rejects or throws an error/warning if the resolved model in Gemini environment does not start with 'gemini-'
  </behavior>
  <action>
    Add test blocks to describe the environment-aware model resolution override in `tests/core.test.cjs`. Mock `process.env.GEMINI_CLI` during testing to simulate the environment. Ensure tests cover validation logic for 'gemini-' prefixes and mapping via config overrides. Specifically test that both short tiers ('opus', 'sonnet', 'haiku') and full Claude names ('claude-3-opus', 'claude-3-5-sonnet', etc.) are correctly mapped.
  </action>
  <verify>
    <automated>npm run test tests/core.test.cjs</automated>
  </verify>
  <done>Failing tests are created for the new required behaviors.</done>
</task>

<task type="auto" status="completed">
  <name>Task 2: Implement Environment-Aware Model Resolution and Config</name>
  <files>get-shit-done/bin/lib/core.cjs, get-shit-done/templates/config.json</files>
  <action>
    1. Update `get-shit-done/templates/config.json` to include the default Anthropic-to-Gemini mapping.
    2. Update `loadConfig` in `core.cjs` to explicitly parse the default model mapping from `config.json` instead of hardcoding.
    3. Update `resolveModelInternal(cwd, agentType)`:
       - Determine base model through existing priority (model override -> profile fallback).
       - If `isGeminiEnvironment()` returns true:
         - Replace known tiers ('opus', 'sonnet', 'haiku') AND their Claude-prefixed variants (e.g., 'claude-3-opus', 'claude-3-5-sonnet') based on parsed config mapping.
         - Validate that the final model name starts with `gemini-`.
         - Output a one-time notification: "Environment detected: gemini-cli. Mapping '{original}' to '{mapped}'."
         - If known deprecated models are used, output a deprecation warning to the user.
  </action>
  <verify>
    <automated>npm run test tests/core.test.cjs</automated>
  </verify>
  <done>All tests pass with mapping implementation applying configured defaults, valid prefixes, warnings, and preserving custom Gemini variants.</done>
</task>

<task type="auto" status="completed">
  <name>Task 3: Implement User Feedback & Visibility</name>
  <files>get-shit-done/bin/gsd-tools.cjs, get-shit-done/bin/lib/commands.cjs</files>
  <action>
    Update CLI command handling to surface the mapped models:
    - Add a `--models` flag to the status/progress commands (e.g. `gsd progress --models`) in `get-shit-done/bin/gsd-tools.cjs` and `get-shit-done/bin/lib/commands.cjs` to display the currently resolved model mappings for each agent.
    - Ensure progress indicators update dynamically to show the actual mapped Gemini model string (e.g., `gemini-3.1-pro-preview`) instead of the base Anthropic tier.
  </action>
  <verify>
    <automated>node get-shit-done/bin/gsd-tools.cjs progress --help | grep -q -- "--models" || node get-shit-done/bin/gsd-tools.cjs status --help | grep -q -- "--models"</automated>
  </verify>
  <done>CLI correctly exposes mapped models through commands and progress output.</done>
</task>

</tasks>

<verification>
Unit tests must successfully execute without errors. The user receives clear notifications about mappings and can view current mappings with `--models`.
</verification>

<success_criteria>
- `opus` and `claude-3-*` variants map to their Gemini equivalents based on `config.json`
- Native `gemini-*` profile names successfully bypass Anthropic checks.
- One-time notification is displayed upon mapping.
- Validation logic enforces `gemini-` prefix in gemini-cli environment.
- `--models` flag added to progress/status command.
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-resolution-mapping/02-01-SUMMARY.md`
</output>
